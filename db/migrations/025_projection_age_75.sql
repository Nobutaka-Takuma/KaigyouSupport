-- 75歳以上を将来推計に足す。
--
-- 歯科では 65歳以上と 75歳以上は別の層です。訪問歯科診療の対象はおおむね
-- 後者で、通院できるかどうかが分かれます。「高齢化率 42%」だけでは、
-- 待合室に来る人が増えるのか、往診に行く先が増えるのかが区別できません。
-- 開業方針としてはまったく別の話になります。
--
-- 公表データ（国土数値情報 500mメッシュ別将来推計人口）は PTD_{年} として
-- 75歳以上を持っています。あるものを取りこぼす理由がありません。
ALTER TABLE mesh_population_projection
    ADD COLUMN IF NOT EXISTS age_75_plus double precision;
